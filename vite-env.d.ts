// The reference to 'vite/client' was removed to resolve a type definition error.
// This project uses `process.env` (polyfilled by the build tool) instead of `import.meta.env`,
// and does not import static assets, so the Vite client types are not required.
